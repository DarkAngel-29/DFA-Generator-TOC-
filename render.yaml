services:
  - type: web
    name: dfa-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y graphviz
    startCommand: python app.py